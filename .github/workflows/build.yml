name: Build cardreader plugin

on:
  push:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Meson and Ninja
        run: pip install meson ninja

      - uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Build
        shell: cmd
        run: |
          meson setup build64 --buildtype=release
          ninja -C build64

      - name: Collect files
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force out | Out-Null
          Copy-Item build64\src\cardreader.dll out\
          Copy-Item dist\cardreader.toml out\

      - uses: actions/upload-artifact@v4
        with:
          name: cardreader-spad0
          path: out

      # Also publish as a GitHub Release so the download never expires
      - name: Zip for release
        shell: pwsh
        run: Compress-Archive -Path out\* -DestinationPath cardreader-spad0.zip

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: Card reader plugin build ${{ github.run_number }}
          files: cardreader-spad0.zip
